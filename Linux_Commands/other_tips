use vim to edit directory filenames:

	install vidir
	type $ vidir
	edit and save


change default editor:

	$ sudo update-alternatives --config editor
	follow instructions

$ echo $? <- type this on the command line after running a c++ program and it will show the 
			"return 0" or whatever number the program returned
